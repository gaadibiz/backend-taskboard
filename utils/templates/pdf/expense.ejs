<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Report Invoice</title>
    <style>
      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        font-size: 14px;
        padding: 40px;
        background: linear-gradient(to bottom right, #eaf4f4, #fefefe);
        color: #333;
      }

      .container {
        max-width: 750px;
        margin: 0 auto;
        background-color: #ffffff;
        padding: 40px 50px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        border-radius: 16px;
        transition: all 0.3s ease-in-out;
      }

      h2 {
        text-align: center;
        margin-bottom: 35px;
        color: #1c3d5a;
        font-size: 26px;
        position: relative;
      }

      h2::after {
        content: "";
        display: block;
        margin: 10px auto 0;
        width: 60px;
        height: 3px;
        background-color: #00796b;
        border-radius: 2px;
      }

      .section {
        margin-bottom: 25px;
      }

      .row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px dashed #d0d0d0;
        align-items: center;
        transition: background-color 0.2s;
      }

      .row:hover {
        background-color: #f6f6f6;
      }
      .label {
        font-weight: 600;
        color: #444;
        width: 45%;
        font-size: 15px;
      }

      .value {
        font-weight: 400;
        color: #222;
        text-align: right;
        width: 50%;
        font-size: 15px;
      }

      .highlight {
        padding: 6px 12px;
        border-radius: 20px;
        font-weight: 600;
        background-color: #e0f7fa;
        color: #00796b;
        display: inline-block;
        font-size: 13px;
        letter-spacing: 0.5px;
      }

      .footer {
        margin-top: 40px;
        text-align: center;
        font-size: 13px;
        color: #888;
        border-top: 1px solid #eee;
        padding-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2><%= expense_type %> REPORT</h2>

      <div class="section">
        <div class="row">
          <div class="label">Status:</div>
          <div class="value"><span class="highlight"><%= status.replace('_', ' ') %></span></div>
        </div>
        <div class="row">
          <div class="label">Expense Type:</div>
          <div class="value"><%= expense_type %></div>
        </div>
        <% if (job_name && job_order_no) { %>
        <div class="row">
          <div class="label">Job Name:</div>
          <div class="value"><%= job_name %></div>
        </div>
        <div class="row">
          <div class="label">Job Order No:</div>
          <div class="value"><%= job_order_no %></div>
        </div>
        <% } %>

        <div class="row">
          <div class="label">Project:</div>
          <div class="value"><%= project_name || '-' %></div>
        </div>
        <div class="row">
          <div class="label">Reimbursed Amount:</div>
          <div class="value">
            ₹ <%= (requested_advance_amount || reimbursed_amount) ?
            (requested_advance_amount ||
            reimbursed_amount).toLocaleString('en-IN') : 'N/A' %>
          </div>
        </div>
        <div class="row">
          <div class="label">Created By:</div>
          <div class="value"><%= created_by_name || '-' %></div>
        </div>
        <div class="row">
          <div class="label">Expense For:</div>
          <div class="value"><%= user_name || '-' %></div>
        </div>
        <div class="row">
          <div class="label">Category:</div>
          <div class="value"><%= expense_category_name || '-' %></div>
        </div>
        <div class="row">
          <div class="label">Expense Date:</div>
          <div class="value">
            <%= new Date(expense_date).toLocaleDateString("en-GB") %>
          </div>
        </div>
      </div>

      <div class="footer">
        © <%= new Date().getFullYear() %> Expense Management System. All rights
        reserved.
      </div>
    </div>
  </body>
</html>
